{
    "name": "@kabootar/git-sync",
    "version": "0.1.0",
    "description": "Git-first collection storage — read, write, diff, and sync collections via Git",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "type": "module",
    "license": "MIT",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "test": "vitest run",
        "typecheck": "tsc --noEmit",
        "lint": "eslint src/",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "@kabootar/core": "workspace:*",
        "isomorphic-git": "^1.25.0"
    },
    "devDependencies": {
        "@types/node": "^20.14.0",
        "typescript": "^5.5.0",
        "vitest": "^2.0.0"
    }
}